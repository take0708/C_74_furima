.header
  = render partial: 'items/header'
.registrations-new
  -# %header.single-header
    -# %h1
    -#   %img{:alt => "mercari", :src => "//pcweb-assets.mercdn.net/assets/img/common/common/logo.svg?1609522297"}/
      -# %nav.progress-bar.signup-bar
      -#   %ol.clearfix
      -#     %li.active
      -#       会員情報
      -#       .progress-status
              
      -#     %li
      -#       商品送付先入力
      -#       .progress-status
      -#         \::before
      -#         \::after

      -#     %li
      -#       完了
      -#       .progress-status
      -#         \::before

  .registrations-new__wrapper
    .registrations-new__wrapper__nav
      会員情報入力
    .registrations-new__wrapper__form
      .registrations-new__wrapper__form__set
        %form#new_user.new_user{"accept-charset" => "UTF-8", :action => "/user_registration", :method => "post"}
        = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
          = render "devise/shared/error_messages", resource: @user
          .registrations-new__wrapper__form__set__nickname
            = f.label :"ニックネーム"
            %span.form-require 必須
            %br/
            = f.text_field :nickname, placeholder: '例）フリマ太郎'
          
          .registrations-new__wrapper__form__set__e-mail
            = f.label :"メールアドレス"
            %span.form-require 必須
            %br/
            = f.email_field :email, placeholder: 'PC・携帯どちらでも可'

          .registrations-new__wrapper__form__set__password
            = f.label :"パスワード"
            %span.form-require 必須
            - if @minimum_password_length
              %em
            %br/
            = f.password_field :password, placeholder: '7文字以上の半角英数字' 
            %br/
            ※ 英字と数字の両方を含めて設定してください
          .registrations-new__wrapper__form__set__checkpassword
            = f.label :"確認用パスワード"
            %span.form-require 必須
            %br/
            = f.password_field :password_confirmation, placeholder: 'パスワードをもう一度入力してください' 
          本人確認
          %br/
          安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .registrations-new__wrapper__form__set__family-name
            = f.label :"お名前(全角)"
            %span.form-require 必須
            %br/
            = f.text_field :family_name, placeholder: '例）山田'
          .registrations-new__wrapper__form__set__first-name
            = f.label :"お名前(全角)"
            %br/
            = f.text_field :first_name, placeholder: '例）彩'
          .registrations-new__wrapper__form__set__family-name-kana
            = f.label :"お名前カナ(全角)"
            %br/
            = f.text_field :family_name_kana, placeholder: '例）ヤマダ'
          .registrations-new__wrapper__form__set__first-name-kana
            = f.label :"お名前カナ(全角)"
            %br/
            = f.text_field :first_name_kana, placeholder: '例）アヤ'
          .registrations-new__wrapper__form__set__birth
            = f.select :birth_year, [*1900..2019].reverse, prompt: ""
            = f.select :birth_month, [*1..12], prompt: ""
            = f.select :birth_day, [*1..31], prompt: ""
          ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .registrations-new__wrapper__form__set__actions
            = f.submit "次へ進む"
= render 'items/btmDl'
.footer
  = render partial: 'items/footer'
= render partial: 'items/exhibitionBtn'